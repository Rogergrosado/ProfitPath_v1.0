## Development Setup

Run `npm install` inside both the `backend` and `frontend` folders to install dependencies. The `.gitignore` file excludes `node_modules/`, so please avoid committing installed packages.
